openpresentations.sql is verandert

bij ON DELETE en ON UPDATE van de tabellen users_has_groups en groups_has presentations heb ik als actie CASCADE ingesteld
zodat we geen last hebben met foreign keys